# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'health_kit_test' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for health_kit_test
  pod 'Alamofire'
  pod 'SwiftyJSON'
  target 'health_kit_testTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'health_kit_testUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
